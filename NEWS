# rlms 0.7
* added option `haven = "numeric"` to the function `rlms_read()`
* added experimental version of `haven = "factor"` to the function `rlms_read()`

# rlms 0.6
* added functions `rlms_show_variable_labels()`, `rlms_show_value_labels()`
* added option `remove_empty = TRUE` to the function `rlms_read()`
